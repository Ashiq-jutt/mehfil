Static assets served at `/assets/...` (gift icons, store item artwork, shop pack images).

Seed data references paths under `gifts/`, `shop/` and `store/<kind>/`. The original
artwork lands here in a later phase; until then the app shows placeholders for missing files.

rules_version = '2';

// Default-deny baseline. Profile photo / room cover / sticker / gift asset
// rules are added with the features that upload them.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
